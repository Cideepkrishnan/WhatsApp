import 'package:flutter/material.dart';

class starred_message extends StatelessWidget {
  const starred_message({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}